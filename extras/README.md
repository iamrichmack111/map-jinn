# Optional OSMnx mode

The main Atlanta map intentionally does not require OSMnx. If an outside-Atlanta mode is added later, install OSMnx separately and use it only for external places. This keeps the Atlanta starter map immediate and avoids rebuilding data that the City already publishes.
