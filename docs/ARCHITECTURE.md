# Map Jinn architecture

The canonical architecture source is [`architecture.d2`](architecture.d2). It uses the custom SVG icon set under [`icons/`](icons/).

![Map Jinn D2 architecture](architecture.svg)

The diagram covers the browser workspace, Python login/server layer, external GIS footprint/street services, print export path, Playwright screenshot/video generation, Docker packaging, GitHub Actions, GHCR publishing, and GitHub Releases.
